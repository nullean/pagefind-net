{
  "name": "pagefind-net-example",
  "version": "1.0.0",
  "description": "Provides the Pagefind query runtime (pagefind.js + wasm) for the pagefind-net example.",
  "private": true,
  "scripts": {
    "emit-runtime": "node emit-runtime.mjs"
  },
  "devDependencies": {
    "pagefind": "1.5.2"
  }
}
